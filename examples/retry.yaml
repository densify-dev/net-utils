# all attributes are optional, if omitted then the default values below are used
# wait_min: 1s
# wait_max: 30s
# max_attempts: 4
# policy: default # valid values: default (same as exponential), exponential, jitter
